#!/usr/bin/env bash
set -euo pipefail

if [[ ! -f "./ios/Podfile" ]]; then
  echo "Error: ./ios/Podfile not found. Run this in the Flutter project root."
  exit 1
fi

rm -rf ./ios/Pods/
rm -rf ./ios/Podfile.lock

flutter clean
flutter pub get

pod install --repo-update --project-directory=./ios
